// 简化的WebSocket服务器启动脚本
// 由于WebSocket服务器已经集成到状态管理存储中，这里只需要确保服务器运行

console.log('WebSocket server is integrated into the application state management.')
console.log('The WebSocket server will start automatically when the application initializes.')
console.log('WebSocket server should be available on port 8080.')

// 保持进程运行
setInterval(() => {
  // 空的间隔，只是为了保持进程运行
}, 1000)